<template>
  <div class="">
    cus storage
  </div>
</template>

<script setup lang="ts">
import { cusStorage } from '@/utils/cusStorage';

// cusStorage.setItem('a', 1);
// cusStorage.setItem('b', 2);
// cusStorage.setItem('c', 3);
// cusStorage.removeExcludeListOfItem(['b']);
// interface Props {}
// const props = withDefaults(defineProps<Props>(), {});

// const emit = defineEmits([]);

// hooks

// ref, reactive

// computed

// function

// watch

// lifecycle

// defineExpose({});
</script>

<style lang="scss" scoped>
</style>
